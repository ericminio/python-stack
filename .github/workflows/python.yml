name: Build

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Display docker version
      run: docker version
    - name: Build images
      run: docker-compose build
    - name: Start
      run: docker-compose up -d
    - name: Run features
      run: docker-compose exec backend ./features/support/run.sh

      