name: Build

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Display docker version
      run: docker version
    - name: Build images
      run: docker-compose build
      